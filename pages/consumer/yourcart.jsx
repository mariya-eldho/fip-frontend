import CartPage from "./CP";

function Cart() {

  return(
    <div
      style={{
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
        height: "100vh",
      }}
    >
        <CartPage />
        </div>
  ); 
};
 export default Cart;